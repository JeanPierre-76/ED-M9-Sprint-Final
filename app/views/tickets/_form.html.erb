<%= form_with(model: ticket) do |form| %>
  <% if ticket.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
        <% ticket.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, "Título de la etiqueta:", style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <% form.label :user_id %>
    <%= form.hidden_field :user_id, value: @user %>
  </div>

  <div>
    <%= form.label :product_id, "Elige un producto:", style: "display: block", class: "mt-3"  %>
    <%= form.collection_select :product_id, @tus_product, :id, :name  %>
  </div>

  <div>
    <%= form.submit "Crear", class: "btn btn-warning mt-4 btn-lg" %>
  </div>
<% end %>
